# 🚀 Развертывание проекта T.Digital

## GitHub загрузка

Поскольку сеть заблокирована в текущей среде, выполните следующие команды вручную:

```bash
# Добавьте remote (если не добавлен)
git remote add origin https://github.com/hojibobohojiboev25-wq/T.Digital.git

# Загрузите все файлы в GitHub
git push -u origin master
```

## 🔍 Проверка перед загрузкой

### Файлы которые НЕ должны попасть в GitHub:
- ❌ `.env` (секретные ключи)
- ❌ `node_modules/` (зависимости)
- ❌ `uploads/` (загруженные файлы)
- ❌ Временные файлы

### Файлы которые ДОЛЖНЫ быть в GitHub:
- ✅ `index.html` - Главная страница
- ✅ `pages/` - Все страницы сайта
- ✅ `css/style.css` - Стили
- ✅ `js/` - JavaScript файлы
- ✅ `admin/` - Админ-панель
- ✅ `languages/` - Переводы
- ✅ `data/content.json` - Данные контента
- ✅ `.env.example` - Пример конфигурации
- ✅ `.gitignore` - Исключаемые файлы
- ✅ `package.json` - Зависимости
- ✅ `README.md` - Документация
- ✅ `LICENSE` - Лицензия
- ✅ `robots.txt` - Для SEO
- ✅ `sitemap.xml` - Карта сайта
- ✅ `404.html` - Страница ошибки
- ✅ `server.js` и `simple-server.js` - Серверы

## 📋 Проверка целостности

### Тестирование сайта:
```bash
# Запуск сервера
node simple-server.js

# Проверка основных страниц:
# http://localhost:8080 - Главная
# http://localhost:8080/admin/login - Админ-панель
# http://localhost:8080/pages/about.html - О компании
# http://localhost:8080/pages/services.html - Услуги
# http://localhost:8080/pages/portfolio.html - Портфолио
# http://localhost:8080/pages/contact.html - Контакты
```

### Тестирование многоязычности:
1. Откройте главную страницу
2. Переключите язык в правом верхнем углу
3. Проверьте что текст меняется

### Тестирование админ-панели:
1. Зайдите в `/admin/login`
2. Введите: `admin` / `admin123`
3. Проверьте все разделы управления

## 🔧 Настройка на сервере

### 1. Загрузка с GitHub:
```bash
git clone https://github.com/hojibobohojiboev25-wq/T.Digital.git
cd T.Digital
```

### 2. Настройка переменных окружения:
```bash
cp .env.example .env
# Отредактируйте .env с реальными данными
```

### 3. Установка зависимостей (опционально):
```bash
npm install
```

### 4. Запуск:
```bash
# Простой запуск
node simple-server.js

# Или с зависимостями
node server.js
```

## 🔒 Безопасность

### Обязательно настройте:
1. **Смените пароль админа** в `.env` файле
2. **Настройте Telegram бота** с вашими ключами
3. **Добавьте HTTPS** в продакшене
4. **Настройте firewall** для защиты сервера
5. **Регулярно обновляйте** зависимости

### Рекомендации:
- Используйте сложные пароли
- Включайте 2FA где возможно
- Мониторьте логи сервера
- Делайте регулярные бэкапы

## 📊 Мониторинг

### Проверка работоспособности:
```bash
# Проверка что сервер работает
curl -I http://localhost:8080

# Проверка админ-панели
curl -I http://localhost:8080/admin/login

# Проверка API
curl http://localhost:8080/api/lang/ru
```

### Логи сервера:
```bash
# Сохранять логи
node simple-server.js > server.log 2>&1 &

# Просмотр логов
tail -f server.log
```

## 🚨 Устранение проблем

### Если сайт не загружается:
1. Проверьте что сервер запущен: `ps aux | grep node`
2. Проверьте порт: `netstat -tulpn | grep :8080`
3. Проверьте логи на ошибки

### Если админ-панель не работает:
1. Проверьте правильность логина/пароля
2. Очистите cookies браузера
3. Проверьте файл `.env`

### Если переводы не работают:
1. Проверьте синтаксис JSON в `languages/*.json`
2. Убедитесь что все ключи совпадают
3. Проверьте консоль браузера на ошибки

## 🎯 Финальная проверка

### ✅ Сайт должен иметь:
- [ ] Главную страницу с hero-секцией
- [ ] Рабочие ссылки навигации
- [ ] Переключатель языков
- [ ] Адаптивный дизайн
- [ ] Форму контактов

### ✅ Админ-панель должна иметь:
- [ ] Страницу входа
- [ ] Панель управления
- [ ] Разделы для редактирования контента
- [ ] Защиту от несанкционированного доступа

### ✅ Безопасность:
- [ ] `.env` файл не в GitHub
- [ ] Секретные ключи защищены
- [ ] Валидация всех входных данных
- [ ] Защита от распространенных атак

---

## 🎉 Готово!

После выполнения всех шагов сайт T.Digital будет полностью функциональным и безопасным! 🚀